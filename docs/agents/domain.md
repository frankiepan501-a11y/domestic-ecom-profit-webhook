# Domain docs

This repository uses a single-context documentation layout.

Before changing business logic, read root `CONTEXT.md` if it exists and any relevant ADR under `docs/adr/`. Use domain terms consistently. If a proposed change conflicts with an ADR, surface the conflict instead of silently overriding it.

Missing domain files are not blockers; create or expand them only when a stable business term or decision needs to be recorded.

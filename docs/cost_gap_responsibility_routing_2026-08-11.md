# 国内电商毛利成本缺口责任分流修复记录

> 说明：2026-08-11 的“运营/采购/Frankie 三路分流”是历史方案，已被 2026-08-12 用户确认的新责任口径覆盖。当前有效规则以本文“2026-08-12 责任口径纠正”为准。

## 问题

2026-07 试算把 3 个天猫宝空订单号作为“SKU/对象”发给采购，要求维护采购成本。消息缺少平台、店铺、对象类型、商品、真实原因和责任判断。

## 根因

- `settlement_engine` 已生成完整缺口行，但 `task_runner` 只抽取对象列。
- 订单资料缺口与 ERP SKU 成本为 0 共用同一个采购通知。
- 订单号再按 SKU 元数据查询，导致名称和店铺为空。
- 3 个目标订单在 2026-05 下单、2026-07 结算；已上传订单明细从 2026-06-01 才开始，因此订单完全未匹配。

## 改动

- 新增 `app/cost_gap_alert.py`：按确定性规则分类、补充订单上下文、分别发送运营/采购卡；责任不明时只发 Frankie。
- 新增 `cards.cost_gap_alert_card()`：运营、采购、待判断三类卡使用不同结论、字段、步骤和处理入口。
- `task_runner` 改为把完整 `settlement + raw` 交给分流器，不再发送旧纯文本采购告警。
- 新增 `COST_GAP_ALERT_FRANKIE_ONLY`，默认 `true`，避免样卡误发业务同事。
- `/health` 增加 `cost_gap_responsibility_routing` 和 `cost_gap_alert_frankie_only`。
- 订单明细未匹配类 P0 也进入运营分流，不再只处理“采购成本”类别。
- 采购分流同时校验 `cost_rows` 中存在同名 ERP SKU 且单件采购成本确为 0；只有问题文案、没有 SKU 证据时留给 Frankie。
- Frankie-only 与正式生产使用不同幂等键；发送未取得 message_id 时不记为已发送，可安全重试。
- 单路缺口超过 25 条时自动分页，每张卡都显示总数/页码；补导范围按全部缺口计算。
- 每条明细直接写平台/店铺；抖音/小红书结算源有下单日期时自动取值，没有时按结算月向前保守回溯 3 个月。
- 采购发送按每名收件人单独防重复；岗位无人时给 Frankie 的兜底提醒不会阻断岗位恢复后的正式派送，部分人员发送失败也只重试失败者。
- 运营群、单个采购收件人或待判断私聊发生网络异常时按路由隔离，不会让后续收件人和其他责任路由一起漏发；失败项不写已发送审计。
- 防重复记录查询发生异常时，为避免重复打扰，只跳过当前这一张卡并记录告警；后续页面、收件人和其他责任路线继续处理。

## 验证

- 33 项单元测试通过，覆盖订单资料→运营、ERP SKU 成本 0→采购、无 SKU 证据→Frankie、天猫/抖音订单日期补充、无日期保守范围、逐条平台店铺、卡片影响说明、分页、样卡/正式幂等隔离、按收件人重试、岗位兜底恢复、单点发送异常隔离，以及防重复记录查询失败时只跳过当前卡、不影响其他责任路线。
- 2026-07 当前资料回放：运营 3 条、采购 0 条、待判断 0 条。
- 运营/采购样卡均通过结构检查：无 form、无回调按钮、各 1 个 URL 处理入口。
- 最终版运营样卡已发给 Frankie 并回读为 `interactive`：`om_x100b689add5ebcbcb2aedf6949f729f`。
- 最终版采购样卡使用虚拟 `SAMPLE-ERP-SKU-001`，已发给 Frankie 并回读为 `interactive`：`om_x100b689add6568b0b1dd921bc096ccf`；未发真实采购。

## 当前上线状态

Frankie 已确认两类样卡通过，允许发布、把生产环境 `COST_GAP_ALERT_FRANKIE_ONLY` 切为 `false`，并重跑 2026-07 初检。发布前代码审查又补上“同一 ERP SKU 跨平台/店铺时必须按平台 + 店铺 + 月份精确匹配成本上下文”的防串店规则。

## 2026-08-12 责任口径纠正（覆盖上文旧分流）

### 用户确认的新规则

- 国内电商毛利报表的订单资料、采购成本、物流成本均由国内电商运营负责补充或协调。
- 平台资料缺口同样由国内电商运营补交。
- 采购不再直接接收本报表的采购成本缺口卡；如需采购信息，由国内运营沟通并统一收口。

### 本次漏发与补救

- 新报表只读核对发现：天猫物流成本 105 行（89 个唯一对象）；抖音宝空、抖音纷岚各缺结算订单或订单明细；原群卡只覆盖小红书 9 个唯一订单缺口。
- 已在国内电商平台沟通群补发一张汇总卡并真实 @ 当前岗位人员赵伟俊，发送后回读群、@、天猫数量、抖音两店和责任文案均通过。
- 补充卡 message_id：`om_x100b688b1894b0a0b2205629e8ec995`。

### 代码修复

- `OPERATIONS_GAP_CATEGORIES` 统一包含 `资料缺口 / 订单明细匹配 / 采购成本 / 物流成本`。
- 自动触发器不再只筛采购成本与订单匹配，避免继续漏掉物流成本和平台资料。
- 已移除采购岗位发送分支；上述四类缺口全部进入运营群卡。
- 运营卡逐条显示类别和对象类型，并保留平台、店铺、问题、影响、建议动作；采购成本为 0 时显示 ERP SKU，物流缺口区分运单号和订单号，资料缺口显示店铺。
- 幂等键升到 `domestic_profit_cost_gap_alert_v4`，避免旧责任路由的已发送记录挡住新口径。

### 验证、纠错与发布

- 36 项全量单元测试通过。
- 2026-07 新报表回放：原始 P0 120 行，去重后运营 102 个（物流 89、资料 4、订单匹配 9），采购 0、财务待判断 0。
- 系统结构自检、发送后回读和双轴代码复审均通过；此卡无输入、审批或回调，不需要 Frankie 充当测试收件人。
- 曾误发到 Frankie 私聊的两张格式样卡 `om_x100b688b39ac38acb04019e79729434`、`om_x100b688bc75028a4b22c3fe2408694a` 已于用户纠正后精确撤回；群内正式补件卡 `om_x100b688b1894b0a0b2205629e8ec995` 保留且回读正常。
- 以后本类信息卡默认直接发国内电商群并按岗位 @；只有带输入、审批、回调或会改变业务状态的交互卡才另做人审测试。
